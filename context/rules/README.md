# Rules & Constraints

## Purpose
Store task-specific rules and constraints for AI coding agents. Use these files to capture environment limits, behavioral guardrails, validation expectations, or local conventions for a given task.

## Usage
Reference these files from the related document in `context/tasks/`.
